# Connection URL for the {{.Database}} database.
# Copy this file to .env and replace the value with your real credentials.
# {{.Database}}.config.ts reads it via env("{{.EnvVar}}").
{{.EnvVar}}="{{.URLExample}}"
