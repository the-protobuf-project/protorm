# Connection URL for the events_db database.
# Copy this file to .env and replace the value with your real credentials.
# events_db.config.ts reads it via env("EVENTS_DB_DATABASE_URL").
EVENTS_DB_DATABASE_URL="mongodb://localhost:27017/events_db"
