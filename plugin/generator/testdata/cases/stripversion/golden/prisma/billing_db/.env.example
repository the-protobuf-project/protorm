# Connection URL for the billing_db database.
# Copy this file to .env and replace the value with your real credentials.
# billing_db.config.ts reads it via env("BILLING_DB_DATABASE_URL").
BILLING_DB_DATABASE_URL="postgresql://user:password@localhost:5432/billing_db"
