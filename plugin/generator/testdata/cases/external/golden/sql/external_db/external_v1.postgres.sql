-- Code generated by protoc-gen-protorm. DO NOT EDIT.
-- versions:
-- 	protoc-gen-protorm (unknown)
-- 	protoc             (unknown)
-- source: external.proto, money.proto
--
-- database: external_db
-- schema:   external_v1
--
-- protorm — https://github.com/the-protobuf-project/protorm

CREATE SCHEMA IF NOT EXISTS "external_v1";

-- Product owns a price expressed as an imported Money value type.
CREATE TABLE "external_v1"."products" (
    -- Unique identifier for the record.
    "id"  CHAR(26)  NOT NULL  PRIMARY KEY,
    -- Resource name; the AIP identifier.
    "name"  VARCHAR(255)  NOT NULL  UNIQUE,
    -- Human-readable product title.
    "title"  VARCHAR(255)  NOT NULL,
    -- Foreign key to Money.
    "price_id"  CHAR(26)  NOT NULL,
    CONSTRAINT "fk_products_price_id" FOREIGN KEY ("price_id") REFERENCES "external_v1"."moneys"("id") ON DELETE CASCADE
);
CREATE INDEX "idx_products_price_id" ON "external_v1"."products" ("price_id");

-- Money is an amount of money with its currency type.
CREATE TABLE "external_v1"."moneys" (
    -- Unique identifier for the record.
    "id"  CHAR(26)  NOT NULL  PRIMARY KEY,
    -- The three-letter ISO 4217 currency code.
    "currency_code"  VARCHAR(255),
    -- Whole units of the amount.
    "units"  BIGINT,
    -- Number of nano (10^-9) units of the amount.
    "nanos"  INTEGER
);
