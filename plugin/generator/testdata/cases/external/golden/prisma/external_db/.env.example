# Connection URL for the external_db database.
# Copy this file to .env and replace the value with your real credentials.
# external_db.config.ts reads it via env("EXTERNAL_DB_DATABASE_URL").
EXTERNAL_DB_DATABASE_URL="postgresql://user:password@localhost:5432/external_db"
