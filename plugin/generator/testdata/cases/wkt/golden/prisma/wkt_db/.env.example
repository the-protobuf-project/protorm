# Connection URL for the wkt_db database.
# Copy this file to .env and replace the value with your real credentials.
# wkt_db.config.ts reads it via env("WKT_DB_DATABASE_URL").
WKT_DB_DATABASE_URL="postgresql://user:password@localhost:5432/wkt_db"
